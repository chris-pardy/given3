{
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.23.0",
    "eslint-config-prettier": "^10.1.1",
    "prettier": "^3.5.3",
    "typescript-eslint": "^8.27.0"
  },
  "scripts": {
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm --filter core build && pnpm -r build",
    "lint": "eslint .",
    "format": "prettier --write ."
  }
}
